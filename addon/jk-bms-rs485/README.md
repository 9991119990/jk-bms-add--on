# JK BMS RS485 MQTT

Reads JK BMS PB protocol over RS485 and publishes Home Assistant MQTT discovery sensors.

Default options are set for the tested local installation. Change MQTT credentials and serial port as needed.
